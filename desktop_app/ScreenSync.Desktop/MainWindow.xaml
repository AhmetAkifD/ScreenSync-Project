<Window x:Class="ScreenSync.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ScreenSync.Desktop"
        mc:Ignorable="d"
        Title="ScreenSync Kontrol Merkezi" Height="300" Width="420"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Margin="30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Vertical" VerticalAlignment="Center" Grid.ColumnSpan="2">
            <Button x:Name="BtnListenPort" Content="Portu Dinlemeye Başla" 
            Click="BtnListenPort_Click" Height="40" Margin="10" 
            Background="#2D2D2D" Foreground="White"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                <Ellipse x:Name="StatusLight" Width="20" Height="20" Fill="Red" Margin="5"/>
                <TextBlock x:Name="TxtLightStatus" Text="Veri Bekleniyor..." 
                   Foreground="White" VerticalAlignment="Center"/>
            </StackPanel>

            <Button x:Name="BtnShowStream" Content="Yayını Başlat" 
            Click="BtnShowStream_Click" Height="50" Margin="10" 
            IsEnabled="False" Background="#4CAF50" Foreground="White" 
            FontSize="18" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
